# 2026-07-18 工作日志

## 真心话抽卡对战小程序 — P0+P1 交付

完成微信小程序「真心话抽卡对战」的 **P0 地基 + P1 单人抽卡**，单机可玩。

### 关键决策（与用户逐项确认）
- 后端：自建 Node 服务（零依赖纯 http，WebSocket 实时对战留 P3）
- AI 怒气值评分：首版仅留接口占位（ai.js 返回 {anger:5,reason:''}），后续接大模型
- 抽卡：老虎机滚动题目片段 → 翻牌(rotateY)显示完整题；**优先抽用户题，无则系统题**
- 系统题：内置 10 道价值观开放简答作普通弹兜底（seed.js）
- 导航：3 tab（抽卡/题库/我的），对战从抽卡页按钮进入
- 视觉：全局中性灰白，仅对战页西部沙漠背景
- 批量导入 UX：粘贴→解析→逐题确认（可编辑题目+怒气值）
- 首版仅文本作答，语音录入留迭代期

### 产出文件
- `mp/`：小程序前端，含 flip-card / slot-machine 组件、index(抽卡首页)/bank(题库)/profile(我的) 页、battle 分包占位（room/fight/result）
- `server/`：Node 后端 src/index.js(store/seed/ai)，首次启动自动 seed 到 data/db.json
- `plan/需求确认书.md`、`plan/开发排期.md`：完整需求规格 v1
- `README.md`：运行说明（启动后端→开发者工具导入→勾选不校验域名→config.js 配置）

### 验证
- 全量 18 个 JS 文件 `node --check` 通过（0 失败）
- 后端重启后自动从种子重新初始化；随机抽题优先用户题逻辑 OK
- 后端已后台运行于 http://127.0.0.1:3000

### 待办（后续迭代）
- P2：题库批量导入逐题确认交互、AI 评分接入
- P3：WebSocket 实时对战（老虎机抽弹、装弹/开枪/受击/阵亡像素动画、倍率选择、血量动画、结算 S/A/B/C/D 评级与经验升级）
